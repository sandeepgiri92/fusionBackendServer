const express=require("express");const router=express.Router();const {createExpense,getExpense,updateExpense,deleteExpense,addExpensePayment,deleteExpensePayment}=require("../controller/expenseController");router.post("/add-expense",createExpense);router.get("/expense/:type",getExpense);router.patch("/expense/:id",updateExpense);router.delete("/expense/:id",deleteExpense);router.post("/expense/:id/payments",addExpensePayment);router.delete("/expense/:id/payments/:paymentId",deleteExpensePayment);module.exports=router;